<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity" android:theme="@style/NoAction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" android:theme="@style/NoAction"/>
        <activity android:name=".ui.SignUpActivity" android:theme="@style/NoAction"/>
        <activity
            android:name=".ui.category.CategoryActivity"
            android:theme="@style/AppTheme"
            android:screenOrientation="portrait"
            android:label="Category"/>
        <activity
            android:name=".ui.detail.DetailActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".ui.home.HomeActivity" android:theme="@style/Home"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>